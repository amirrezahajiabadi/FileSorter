# tkinterdnd2 enables drag & drop (dropping a folder onto the window).
# It's optional at runtime: if it's not installed, the app still runs
# fine, just without drag & drop (falls back to a plain Tk window).
tkinterdnd2

# ── Dev-only ──────────────────────────────────────────────────────
# To build a standalone .exe with PyInstaller:
#   pip install pyinstaller
#   pyinstaller --onefile --windowed --clean --name "FileSorter" main.py
#
# To run the test suite:
#   pip install pytest
#   pytest
